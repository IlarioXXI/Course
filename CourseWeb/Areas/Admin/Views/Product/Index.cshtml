@model List<Product>

<div calss="container">
    <div class="row pt-4">
        <div class="col-6 pb-3">
            <h2 class="text-primary">Product List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Product" asp-action="Upsert" class="btn btn primary bg-primary">
                <i class="bi bi-plus-square-fill"></i>Create New Product
            </a>
        </div>
    </div>
</div>

<table id="tblData" class="table talble-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>
                Title
            </th>
            <th>
                Author
            </th>
            <th>
                ISBN
            </th>
            <th>
                Price
            </th>
            <th>
                Category
            </th>
            <th>

            </th>
        </tr>
    </thead>

        @* @foreach (var obj in Model)
        {
            <tr>
                <td>@obj.Title</td>
                <td>@obj.Author</td>
                <td>@obj.ISBN</td>
                <td>@obj.Price</td>
                <td>@obj.Category.Name</td>
                <td>
                <td>
                    <div class="w-75 btn group" role="group">
                        <a asp-controller="Product" asp-action="Upsert" asp-route-Id="@obj.Id" class="btn btn-primary mx-2">
                            <i class="bi bi-pencil-square"></i>Edit
                        </a>
                    </div>
                </td>
                <td>
                    <div class="w-75 btn group" role="group">
                        <a asp-controller="Product" asp-action="Delete" asp-route-Id="@obj.Id" class="btn btn-danger mx-2">
                            <i class="bi bi-trash3"></i>Delete
                        </a>
                    </div>
                </td>
            </tr>
        } 
        senza data table 
        *@

</table>

@section Scripts{
    <script src="~/js/product.js"></script>
}
